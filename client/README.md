# Client

The client will provide the responsive, mobile-capable React + TypeScript user interface for the operation-based calculator. It will render the backend-generated operation manifest and use the shared declarative validation model.

No frontend implementation is included in this bootstrap task.
